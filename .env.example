# Gemini image editing (used by the gemini_edit stage to make the new-object reference).
# Get a key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional alias — GOOGLE_API_KEY is also accepted if GEMINI_API_KEY is unset.
# GOOGLE_API_KEY=
